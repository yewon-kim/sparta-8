경로 @app.route('/list')
GETTING DATA
1. client to api
	$.ajax GET
2. Flask to db
	articles - db.articles.find()
3. api client
	return articles

경로 @app.route('/create')
POSTING DATA
1. client to api
	$.ajax POST
2. db
	db.articles.insert_one()